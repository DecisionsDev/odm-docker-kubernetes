{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost",
      "pattern": "^https://jwt.io"
    }
  ],
  "replacementPatterns": [
      {
        "pattern": "^/LICENSE",
        "replacement": "{{BASEURL}}/LICENSE"
      }
    ],
    "timeout": "20s"
  }
